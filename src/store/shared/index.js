const state = {
  skills: [],
  specialties: []
};

const mutations = {};
const actions = {};
const getters = {};

const shared = {
  state,
  mutations,
  actions,
  getters
};

export default shared;
