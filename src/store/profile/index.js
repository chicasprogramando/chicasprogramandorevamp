const state = {
  profile: {},
  profileList: []
};

const mutations = {};
const actions = {};
const getters = {};

const profile = {
  state,
  mutations,
  actions,
  getters
};

export default profile;
